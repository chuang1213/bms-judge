# Phase 3.5 报告：代码审阅与实验路径反思

> **任务进度补记（2026-09-07 晚）**：本报告 §4 列出的五项建议，当日已完成三项——
> ① **C1/C2 已在 18 玩家重跑**（C1 11.94±0.72 / C2 11.32±0.61，仍显著劣于 C0 8.18，cR² 为负）；
> ② **人为指标审计 + v2 特征**（详见 EXPERIMENT_LOG 2026-09-07 两条：`peak_nps_1s` 被证实纯冗余、
>    B_v2 全线最优、few-shot 全 k 段改善）；
> ③ **ST4-7 诊断**：原"唯一负区域"在 18 玩家下不可复现，上采样无增益，现象消失。
> 仍未做：C0 条件化进 few-shot（最高价值）、头条指标切换（待用户拍板）、推理工具。

> 日期：2026-09-07 | 分支：phase3 | 状态：**代码审阅已完成并修复；方向建议待用户裁决**
> 口径：PROTOCOL.md 全 scope（sl/st/発狂2018 围栏、无难度表特征、c_jrank 在列）、
> 17 玩家、train 6,269 / test 6,278、严格时间外推、历史严格早于目标首打。
> 代码改动见 commit `bd2a565`..`3164a3b`；数字台账见 `EXPERIMENT_LOG.md`。

---

## 0. 一句话结论

**代码层**：修掉 4 个真问题（其中 2 个会静默污染结果、1 个让 SOP 第 4 步完全跑不起来），
并把 5 份重复的特征清单与评估代码收敛到单一登记处，全程 byte-identical 验证。
**方向层**：基线并未被低估——B 比"直接猜玩家均值"好 2.99 acc MAE（30%），路线是健康的；
但**聚合 MAE 主要衡量"能否区分玩家强弱"，不是项目真正关心的 interaction**，而
**C 路线的差距正在系统性收窄（3.42 → 1.94 → 1.56 → 1.25）**，应当据此重排优先级而非维持原判断。

---

## 1. 代码审阅：已修复

| # | 问题 | 影响 | 修复 |
|---|---|---|---|
| 1 | `chart_repr.OBJECTIVE_STAT_COLS` 缺 `c_jrank`（26 项 vs registry 声明的 27 维） | **登记处与实际特征脱节**：每个脚本各自维护一份清单，加特征要改 5 处，正是 `c_jrank` 漏登记的机制 | 补 `c_jrank`；新建 `common.py` 收敛评估原语；5 个脚本改为 import |
| 2 | `c_chart_aware.py` 的 val 切分用**全局** `len(tr_all)//40 = 156` 行 | **6/17 玩家（plus/yangtao/stella/anshi/NIGHT/hl）的全部 train 行变成验证集，训练贡献 0 行**，却在 test 里被评估 | 改为每玩家取各自 train 的最后 10% |
| 3 | `data.py` 打开 JSON 未指定 `encoding` | **`python data.py` 在本机直接 UnicodeDecodeError** —— 新玩家 SOP 第 4 步（重建数据集）完全不可用 | 显式 `encoding="utf-8"` |
| 4 | `coverage_audit.py` 硬编码 `PLAYERS = [4 人]`、`n_players == 4` | 覆盖审计**静默丢弃后来加入的 13 名玩家**，持续产出错误的覆盖结论 | 名单改为从数据推导，配色/图例自适应 |
| 5 | `requirements.txt` 只有 torch/numpy | 新环境无法复现（缺 pandas/sklearn/scipy/pyarrow/matplotlib） | 补全并分组注释 |

**重构的等价性验证**（这是本项目最该坚持的纪律，本轮全程执行）：

| 产物 | 结果 |
|---|---|
| `compare_nolevel.json` | **byte-identical**（A 11.793 / H 7.550 / B 7.011） |
| `transfer_results.json` | **byte-identical**（M2 exp1 acc 7.30；few-shot M2@10 15.49 / @50 12.56 / @200 11.17） |
| `data.py` 重建后数据集 | **byte-identical** |
| 测试 | 25 → **34 个**，全部通过 |

新增 `bms_ml/tests/test_phase3_registry.py`（9 个用例）专门防 #1 类问题：断言 registry 声明维度
== 清单长度、断言无难度表特征、断言 few-shot 子集关系、断言 `centered_r2` 的交互性质
（只预测玩家均值必须得 **恰好 0**，完美预测得 **恰好 1**）。

### 1.1 修复 #2 的实测影响（17 玩家，3 seeds）

| | acc MAE | centered R² | lamp MAE |
|---|---|---|---|
| C0 修复前 | 8.57 ± 0.10 | +0.12 ± 0.01 | 1.42 ± 0.01 |
| **C0 修复后** | **8.262 ± 0.116** | **+0.129 ± 0.031** | **1.348 ± 0.007** |
| H（参照） | 7.550 | +0.281 | 1.658 |
| B（参照） | 7.011 | +0.380 | 1.315 |

C0 变好了，但 **H > B > C 的层级没变** —— 这个 bug 不是 C 落败的原因。诚实记录。

---

## 2. 代码审阅：发现但**没有**改（属于方案级，等你决定）

### 2.1 表围栏被误用在历史上（本轮最重要的发现）

`PROTOCOL.md §1` 与 `PHASE3_AUDIT §12` 都写明围栏作用于**目标**（"训练/评估目标限定…并集内的谱面"），
但 `data.py` 在构造历史**之前**就施加了围栏，于是它同时审查了模型对玩家能知道什么：

```
可解析首打          38,120
  表内（现行口径）  25,104
  表外但可解析      13,016  = 34.1%   ← 从未进入任何 h_* 特征
```

逐玩家丢弃比例：yangtao **66.4%**、hl **53.6%**、reiaki 48.8%、muiclac 48.6%、
vsoflan 47.7%、LED 44.4%。

我没有擅自改行为，而是加了 `HISTORY_USES_OFFTABLE`（默认 `False` = 现状，
可用 `P3_HISTORY_OFFTABLE=1` 覆盖）并实测：

| | acc MAE | centered R² | lamp MAE | BP |
|---|---|---|---|---|
| H 基线 | 7.550 | +0.281 | 1.658 | 148.4 |
| H 放开历史 | 7.815 | +0.271 | 1.722 | 146.3 |
| B 基线 | 7.011 | +0.380 | 1.315 | 129.5 |
| **B 放开历史** | **6.942** | **+0.375** | 1.365 | 128.3 |

**结论：不是增益。** H 全面变差（14/17 玩家更差）——表外谱面的 acc 分布不同，
污染了 `h_acc_mean` 这类无条件统计；B 总体只赚 0.069 且 **centered R² 不动**（+0.380 → +0.375）、
lamp 退步，逐玩家 9 好 8 坏（muiclac −0.64、tzh −1.14、steve −0.89 受益；
darklord +1.13、nanji +0.89、yangtao +1.28 受损）。

**建议：维持现状（默认关）。** 但值得记一笔——若将来按玩家异质性分别处理
（表外占比高的玩家单列），这里可能还有东西。

### 2.2 其余（未改，仅记录）

- **`data.py` 的 kNN 标准化 scaler 在全量 `fp` 上 fit**，含 test 期才首打的谱面统计。
  语义上极轻微（只是标准化，不含标签），但与 `transfer_eval.py` 里
  "scaler 只用训练玩家谱面统计"的约定不一致。属协议级口径问题，需你决定是否统一。
- **`baseline.py` 会直接崩**：它在模块层读取已删除的 `h_level_acc` 列，
  且 docstring 未标 deprecated（`baseline31.py`/`c_model.py` 都标了）。建议补标记，或删。
- **`time_ablation.py` 的 masked 用 `np.isin(时间数组, 隐藏时间)`**：时间戳重复时会多隐藏，
  实际影响很小，但严格来说应该用行索引而非时间值。
- **历史只含表内首打，而 `h_plays_last30d` 却统计全部 scorelog 行**（含 course 行以外的全部）——
  两个"活动度"口径不一致，属既有设计选择，未改。

---

## 3. 实验路径反思

### 3.1 先补一个被省略的检查：模型到底比"猜玩家均值"强多少？

项目一直拿 A/H/B 互比，但没有拿它们比过一个**平凡估计器**。补上（test band）：

| 预测器 | acc MAE |
|---|---|
| 全局均值 | 11.430 |
| **每人 train 均值** | **9.997** |
| 每人最近 20 次均值 | 10.032 |
| A（chart-only HGB） | 11.793 |
| H | 7.550 |
| **B** | **7.011** |

两个结论：

1. **路线是健康的**：B 比"知道这个玩家的平均水平"还好 **2.99 acc MAE（30%）**，
   比最近 20 次均值好 3.02。模型确实在学"这个玩家 × 这张谱"的东西，不是玩家强度的重述。
2. **A（11.793）比全局均值（11.430）还差。** 27 维谱面特征上_fit_出来的东西泛化不如一个常数——
   因为 acc 边际上几乎不由谱面决定，模型在拟合噪声。**PROTOCOL 把 A 当"下界"是误导性的：
   真正的下界是均值预测器，A 在它之下说明 chart-only 在 acc 上是负价值的。**
   建议把 A 的角色从"下界"改成"对照：谱面信息的边际价值"，并引入均值预测器作为真下界。

### 3.2 聚合 MAE 主要衡量"能否区分玩家"，不是 interaction

```
逐玩家 acc 均值跨度     48.8 ~ 88.1（标准差 10.1）
test 集 acc 总体标准差  16.09
B 的 acc MAE            7.011
```

**玩家均值的离散度（10.1）比模型的 MAE（7.0）还大。** 而且误差高度集中：

- nanji 一人贡献总绝对误差的 **17.8%**（其 MAE 15.40，约为中位数玩家的 2.5 倍）
- nanji + yangtao 合计 **21.6%**

也就是说，聚合 MAE 有相当一部分是"能不能把玩家分开"决定的，而项目的核心问题
（player × chart interaction）是**玩家内部**的事。项目早就知道这点——所以才有 centered R²——
但**对外引用的头条数字一直是那个被玩家间方差主导的 MAE**。

**建议（协议级，需你同意）**：把 **centered R² 升为 acc 的头条指标**，
raw MAE 降级为"绝对标定"辅助指标，并新增一个**玩家内 MAE**（y 与 pred 都先减去玩家均值）。
这不会改变任何实验，只改变我们读哪些数字下结论。

### 3.3 C 路线：差距其实在系统性收窄——但收窄得不够快

把台账里历次 C0 与 B 的对比拉成序列（同协议、全 scope、同切分）：

| 玩家数 | train 样本 | C0 acc | B acc | **C0 − B** | C0 centered R² |
|---|---|---|---|---|---|
| 4 | 1,982 | 13.69 | 10.27 | 3.42 | −0.44 |
| 8 | 4,161 | 8.96 | 7.02 | **1.94** | +0.08 |
| 10 | 4,400 | 8.94 | 7.38 | **1.56** | +0.08 |
| **17（修复后）** | 6,269 | **8.262** | **7.011** | **1.25** | **+0.129** |

（4 人那行取自 `PHASE3_3_READINESS.md` §8，该报告带"泄漏修复前"更正横幅，
绝对值可能偏乐观，故 8 人之后的序列更可靠。）

**这与"learned encoder 随数据改善"的假设一致，而且比之前记录的判断更乐观**：
差距单调收窄 3.42 → 1.94 → 1.56 → 1.25，centered R² 已由负转正并持续上升。
但线性外推下要抹平 1.25 大约还需要 **30–50 名玩家**——
**不支持"再招 10 个人 C 就能翻盘"**。

**因此建议改判 C 的定位**：不要再把"打败 H/B"当作 C 的及格线（那是 30+ 玩家之后的事），
而应按 `PHASE3_4_TRANSFER.md §10.1` 的思路，**把 C 用作 M2 的 conditioning 表示替换件**，
用 few-shot 曲线（M2@k）当评审——那里基线更弱（k=1 时 M1 达 39.93）、价值更明确、
且不需要等规模。本轮代码整理后这条路的基础设施已经齐了。

### 3.4 transfer 结果真正说明的事

| 模型 | acc MAE | 训练时是否见过该玩家 |
|---|---|---|
| B（协议内） | 7.011 | 见过（含其 train 行） |
| M2（cross-player） | 7.30 | **从未见过** |

**"这个玩家自己的训练数据"的边际价值 ≈ 0.29 acc MAE。** 而训练时完全没见过该玩家的 M2
已经拿到 7.30。这是一个很强的信号：**个体自有数据的边际收益很低，人群先验 + 个体 conditioning
才是主要价值来源。** 它同时解释了为什么"给每个玩家学一个 encoder"这条路天花板有限——
可学的个体信息本来就只有这么多。

顺带：这与 §3.1 并不矛盾。B 比"玩家均值"好 2.99，其中绝大部分来自**跨玩家共享的结构**，
只有约 0.29 来自该玩家自己的数据。

### 3.5 三目标的信息结构（17 玩家复核，与 3.1 结论一致）

| 目标 | A | H | B | 主导信息 |
|---|---|---|---|---|
| acc | 11.793 | 7.550 | **7.011** | 玩家状态（A 连常数都不如） |
| lamp | 1.749 | 1.658 | **1.315** | 两者都要 |
| BP | 180.1 | 148.4 | **129.5** | 两者交互 |

---

## 4. 思路上我认为可以改进的地方（按价值排序）

1. **把 C 从"打 H/B"改判为"替换 M2 的 conditioning"**（§3.3）。
   这是本轮最重要的方向性建议：同样的模型，换一个它真能赢的赛场。
2. **头条指标换成 centered R² / 玩家内 MAE**（§3.2）。
   零实验成本，但会让"我们进步了吗"这个问题变得可回答。
3. **把 `h_knn_acc` 升级成"玩家在若干谱面轴上的响应剖面"**。
   现在玩家侧只有 1 个与谱面相关的特征（kNN 均值），而它恰恰是最有价值的那个。
   改成沿 密度 / LN / scratch / 时长 / 和弦 等轴的**分轴历史表现**，
   直接针对 ST4-7 的失败模式——那个区域正是"人群平均抹平了个体差异"的地方。
   便宜、可解释、且是对症的。
4. **ST4-7 诊断实验**（`PHASE3_4_TRANSFER.md §10.2` 已列，仍未做）：
   上采样 vs 不做，区分"数据少"还是"玩家异质"。现在 ST8-12 已有 100+ 首打（本轮覆盖审计），
   条件比当时好。
5. **重跑 C1/C2**（17 玩家）。台账"待观察"里写着它们何时反超 C0，
   但上次跑还是 8–10 玩家、且在 val 切分 bug 之前。现在约 5 分钟就能补上这个空白。
6. **尝试残差目标**：直接预测"该玩家这张谱相对其自身均值的偏差"，
   而非绝对 acc。小数据下可能更稳，且天然对齐 interaction 的提问方式。

---

## 5. 建议**停止**做的事

1. **别再堆手工历史标量**（12 → 20 维）。H 已接近天花板，且 §3.4 说明个体信息本就有限。
2. **别再单纯靠"加玩家"期待 C 翻盘**。§3.3 的曲线说需要 30–50 人才抹平 1.25。
   招人应该服务于**覆盖**（ST 高段、短历史新玩家）而不是**总量**。
3. **别把 A 当下界引用**（§3.1）。

---

## 6. 本轮新增的数字（供台账引用）

- 17 玩家全 scope：A **11.793** / H **7.550** / B **7.011**；
  centered R² H **+0.281** / B **+0.380**；lamp B **1.315**（QWK 0.677）；BP B **129.5**
- 平凡基线：全局均值 **11.430** / 玩家均值 **9.997** / 最近 20 次 **10.032**
- C0（修复后，3 seeds）：**8.262 ± 0.116**，cR² **+0.129 ± 0.031**，lamp **1.348 ± 0.007**
- LOPO 冷启动（全 scope）：B acc **7.609**（对照 mean-only 10.996）
- 时间消融（17 玩家复现）：H_time **7.55** / cR² **+0.281** vs H_no_time **8.88** / cR² **−0.002**；
  masked_100 与 H_no_time 精确重合（机制 sanity anchor 成立）
- 覆盖：ST8-12 由报告时的 22 条首打增至 **100+**（不再是 very sparse）
