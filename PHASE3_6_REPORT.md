# Phase 3.6 报告：个人响应剖面与一次数据修复

> 日期：2026-09-11 | 分支：`phase3.6` | commit：`227aa94` → `d0c5d79`
> 口径：PROTOCOL.md 全 scope（sl/st/発狂2018 围栏、c_jrank 在列）、**去重后数据集**
> train 6,358 / test 6,367、严格时间外推、历史严格早于目标首打。
> 数字台账：`EXPERIMENT_LOG.md`（2026-09-11 共 9 条）；特征登记：`chart_repr.py`。

---

## 0. 一句话结论

**玩家侧第一次出现真正有效的表示改进**：把"这个玩家在某个谱面轴上会打多少分"显式建模成
一条可估计的响应曲线（`h_resp_*` / `h_slope_*`），使 acc **7.009 → 6.501（−7.2%）**、
lamp **1.325 → 1.227**、BP **132.9 → 119.5（−10%）**、centered R² **+0.386 → +0.434**，
**16/18 玩家改善**，并通过三重对照（容量、逐玩家、逐轴）。

同期用**五个定位清晰的负结果**排除了歧路，并修掉一个**重复样本 + 自泄漏**的数据 bug。

---

## 1. 当前最优与证据链

### 1.1 数字（同一行集，HGB 确定性 → seed_std = 0）

| 配置 | 维度 | acc MAE | acc cR² | lamp | QWK | BP raw |
|---|---|---|---|---|---|---|
| A（chart-only） | 27 | 11.959 | −0.379 | 1.751 | 0.431 | 180.2 |
| H（history） | 12 | 7.601 | +0.274 | 1.663 | 0.409 | 146.7 |
| **B（参照）** | 39 | 7.009 | +0.386 | 1.325 | 0.670 | 132.9 |
| B_v2 | 53 | 6.796 | +0.408 | 1.301 | 0.686 | 139.1 |
| B_full（acc 块全量） | 63 | 6.583 | +0.430 | 1.331 | 0.670 | 124.7 |
| **`B_resp`（推荐基准）** | **76** | **6.501** | **+0.434** | **1.227** | **0.710** | **119.5** |
| `B_resp_noslope` | 65 | 6.647 | +0.423 | **1.219** | **0.715** | 123.2 |

`B_resp` = v1 27 + 手工历史 12 + **acc 响应块 24** + **lamp 响应块（仅 chart-conditioned）13**
（`response_eval.py` 的 `B_full+lampresp` 行）。lamp 单独最优见 `B_resp_noslope`。

### 1.2 方法

`response_features.py`（唯一权威实现）：对每个玩家、每个轴，用**严格早于目标**的历史做一元 OLS
`target ~ α + β·axis`（前缀累积和，O(n)），再在目标谱面自身的轴取值上求值：

- `<p>resp_<axis>`：**chart-conditioned** 预测（用的是已知的目标轴值，非泄漏）；
- `<p>slope_<axis>`：**纯玩家特质**，`β × 全谱轴标准差`（"每 1 SD 难度损失多少分"）；
- 11 个轴：密度/LN/scratch/时长/和弦/jack ＋ v2 的 ioi / nps_std / simul / lane_entropy ＋ `c_jrank`。

按目标分块（`h_` acc / `l_` lamp / `b_` BP），clip 到各目标值域。

### 1.3 三重证据（缺一不可）

| 证据 | 结果 |
|---|---|
| **容量对照** | 同样 24 列、同分布但**打乱样本配对** → `B+shuffled24` **7.001 ≈ B 7.009**；lamp 块跑 **4 次独立打乱**（lamp 1.325–1.339）→ 真块 1.227 落在分布之外 |
| **逐玩家** | acc **16/18 改善**（top tzh −2.28、MRCA −1.04、stella −0.95；反例 vsoflan +0.54） |
| **逐轴消融** | 留一损失最大 `npsstd` +0.150、`jack` +0.068、`nps` +0.064；单轴最强 `nps` −0.251、`npsstd` −0.248 |

**加性验证**：`h_resp_*` 单用 6.753、`h_slope_*` 单用 6.832，**合并 6.583** → 增益来自**跨轴模式的超加性**。

---

## 2. 负结果清单（均已在台账定位，勿重复投入）

| 方向 | 结果 | 关键数字 |
|---|---|---|
| `perm_space`（排列空间手部几何，22 维） | 胜 v1 基线但**被 v2 支配**；叠加 v2 无增益；few-shot 更差 | B 6.990→6.882，但 B_v2 6.825 < B_v2_perm 6.866 |
| **felt-time 归一化**（framework paper 主张） | **不适用**：样本空间内 NPS **零膨胀** | `c_peak_nps_1s` max **77**（全库 63,900 只在表外） |
| 响应剖面接入 **few-shot** | **净负**（含训练/评估同口径 + 收缩 λ=20） | k=5 18.97 vs 基线 17.50；k=1 的"改善"是常数干扰效应 |
| **残差目标** | **净负**，连它该改善的 cR² 都退 | plain 6.583/+0.430 → residual 7.321/+0.376 |
| **BP 响应块** | **不成立** | BP 119.5→121.0；acc 差异 7 好/5 差/6 平（平均 +0.004，噪声） |

**副产品**：`h_knn_acc` 的 k 债**结案**——无响应剖面时 k 影响明显（k=20 恰是最差），**有响应剖面后
k 几乎不影响**（6.549–6.637）→ 响应剖面吸收掉了 kNN 邻域的作用，维持 k=20。

---

## 3. 数据修复：重复 sha256 → 样本双计 + 自泄漏

`manifest.jsonl` 含 **329 个重复 sha256**（同一谱文件在语料库里存有 2–3 个路径副本，如
`BMS/gremlin_ogg/x.bms` 与 `BMS/GREMLIN/x.bms`），`data.py` 按 sha256 merge 时把首打事件放大：

- `samples` 12,859 行中 **242 行是重复键**（**test 128 = 2.0% 被双计**）；
- 更隐蔽：两份副本**时间戳相同**，排序后其中一份的历史窗口里含**目标自己** → `h_knn_acc` 自泄漏
  （约 121 行）。

修复 = `load_manifest()` 按 sha256 去重 ＋ `main()` 不变量断言（`PROTOCOL.md §1` 已写成硬红线）。
**影响量化**：firstplays 25,728→25,458、samples **12,725**；A 11.973→11.959 / H 7.619→7.601 /
**B 6.990→7.009** → **bug 真实但对头条数字影响约 0.02**。

> ⚠️ 因此**本报告之前的所有 cross-player / few-shot / v2 / perm 数字都在旧 12,859 行数据集上**
> （差异量级 ~0.02）。规范 few-shot 曲线已在去重数据上刷新：v1 `k=1 23.81 / k=200 11.20`；
> **v2 优势依然成立**（`k=1 22.71 / k=200 10.96`）。

---

## 4. 方法论记录（值得写进肌肉记忆）

1. **单次打乱抽样不构成容量对照**。首个打乱抽样给出 lamp 1.238（真块 1.227），差点把 lamp 增益
   误判为容量效应；4 次独立打乱后真实分布是 1.325–1.339。**同宽度随机列在单一抽样上可以摆动 0.1**。
2. **"把 prefix+targets 一起送进批量估计器"是泄漏**：前一个目标会成为后一个目标的历史。症状是
   **k=0（prefix 为空、特征本该全 NaN）却出现 2.4 acc 的"提升"** —— 边界情形是最好的探针。
3. **树模型只对"同一"仿射变换不变**（`c0_state_hgb` 的 chart 尺度 bug，见 §5）。
4. **numpy fancy-index 的 `|=` 会丢重复下标**（`a[idx] |= v` 先取值再整体写回）：它把 2,071 notes 的
   所有和弦静默压成单音。用 `np.bitwise_or.at`。
5. **评估器必须先验"列数涨了会不会白涨"**：`perm_space` 与 `B_resp` 的分野就在这一步。

---

## 5. 本阶段同时修正的两处旧结论

1. **`c0_state_hgb.py` 的 chart 尺度 bug**（commit `227aa94`）：训练帧喂原始 chart、评估帧喂标准化
   chart → 内联 M2 系统性偏坏，伪造成"learned state 每个 k 都赢"。修后内联 M2 与 `transfer_eval`
   官方曲线**逐点吻合**（k=1 23.87 vs 23.874），**原结论推翻**（增益退化为噪声级）。
2. **felt-time 线索关闭**（见 §2）。

---

## 7. 附录：LR2 接入与"客户端分对待"（2026-09-11 晚）

用户提供了第一份真实 LR2 存档（`PlayerData/LunaticRave2/V_soflan.db`），并提出"LR2 与 beatoraja
判定机制类似但不完全一样，考虑区分对待"。结论：**该判断成立，且已量化**。

### 7.1 LR2 存档的结构（实测）

| 项 | 结论 |
|---|---|
| 规模 | `score` 表 6,439 行，**一行 = 一张谱的最佳成绩**（非逐局） |
| 时间戳 | **完全没有**（`player.playcount` = 14,793 是唯一"次数"痕迹） |
| 关联语料库 | 84.2%（5,424 行）经 manifest 的 md5→sha256 桥接，库内无重复 |
| acc | `(PG×2+GR)/(2×notes)`，对存档 `rate` 验证（max\|Δ\|=0.999 = 整数截断，corr 0.9999） |
| lamp | `clear ∈ {0..5}`，**无 FC/PERFECT** |
| 独有能力 | `playcount/clearcount/failcount`、`op_history`、`rseed` |

### 7.2 客户端差异实测（4,576 张共有谱面，统一 manifest 分母）

| 指标 | 值 |
|---|---|
| acc 差（LR2最佳 − beatoraja最佳） | 均值 **+2.14** / 中位 +0.46 / **sd 10.16** |
| 差异幅度 | \|差\|>1pp **58.5%**、>5pp **29.4%**；corr 0.84 |
| 非 LN 谱（n=2,241） | 均值 +4.08 / sd 8.35 |
| corr(\|差\|, ln_ratio) | **+0.016** → 不是 LN 计分口径差异 |

**sd 10.16pp > 模型自身 acc MAE 6.5** → 混池等于注入与信号同量级的噪声。**禁止混池。**

### 7.3 已落地

1. **`lr2_reader.py`**（不再是 stub）+ `ingest_player.py --client lr2`；
2. **权威枚举**取自 `ref_repo/beatoraja-master`（`ScreenShotExporter.java`、`LR2SelectSkinLoader.java`）：
   beatoraja 在 2/3 插入 ASSIST_EASY / L_ASSIST，同一档位**差 +2** → `lr2_clear_to_beatoraja()`
   **仅供对比，不代表可混用**；
3. **`data.py` 客户端守卫**：include=true 且 client≠beatoraja 直接拒绝构建（已单测，防止静默污染）；
4. `PROTOCOL.md §1` 已把"客户端是声明维度 / 禁混池"写为红线；测试 40→**44**。

### 7.4 未决（需用户决策）

- **LR2-only 玩家没有"首打"目标**（一行 = 最佳成绩）→ 当前协议无法纳入。`vsoflan_lr2` 保持
  include=false。
- 一个值得注意的契合：**矩阵补全（ALS）不需要时间戳**。LR2 的"缺点"（无时间、无首打）恰恰是
  `ref_repo/AlphaOSU-main` 所用形式（ALS 分数模型 + pass 模型 + 推理）的**前提**。若要做推荐方向，
  LR2 数据反而比 beatoraja 更直接可用 —— 这可能是后续把"预测"转成"推荐"的入口。

---

## 8. 指数遗忘：响应剖面不该等权（2026-09-11 晚）

响应剖面是当前最大增益来源（B 7.009 → 6.495），但它用**全档案等权 OLS**。这与项目自己的实测
相矛盾：时间特征价值 ~1.33 acc MAE、去掉则 cR² 崩到 0。

### 8.1 动机（实测）

| 量 | 值 |
|---|---|
| 档案跨度 | 中位 **959 天**、最长 2000 天 |
| 玩家 acc 全程漂移 | 均值 **+8.35pp**、sd 11.77、**13/18 人 >5pp**（tzh +41.9、yangtao −15.5） |

等权 OLS 估的是"终生平均响应"，而目标全在最近的 1/4 段。

### 8.2 结果（`response_decay.py`，同一配置 `B_resp+bpresp`）

| 半衰期 | acc | cR² | lamp | BP |
|---|---|---|---|---|
| uniform | 6.495 | +0.438 | 1.222 | 121.0 |
| 720d | 6.430 | +0.436 | 1.202 | 119.7 |
| 365d | 6.494 | +0.433 | 1.204 | 120.4 |
| **180d** | **6.360** | **+0.443** | **1.197** | 122.1 |
| 90d | 6.419 | +0.437 | 1.218 | 121.5 |
| 30d | 6.664 | +0.423 | 1.243 | 122.7 |

**配对检验（6,367 行）**：mean \|err\| 改善 **+0.136**，95% CI **0.071..0.195**，t=4.25，
**p=2.2e-5**，**52.7% 的样本变好** → 不是离群驱动。3-seed sd = 0.000（HGB 在此配置确定性）。
30d 覆盖跌到 0.938 → 有效样本不足，是收益反转的原因。

复现：`P3_RESP_HALF_LIFE=180 python bms_ml/phase3/history_response.py`（默认仍是等权）。

### 8.3 过程中的两个 bug（都被防住了）

1. **`0*NaN` 污染 cumsum**：加权改写时把 `xv` 从 `np.where(v,x,0)` 改成裸 `x`，v2 轴里 28 个
   NaN 经前缀和传播，使 4 个 v2 轴 **86% 变 NaN**（B_full 6.583→6.771）。被"新算的块必须逐点
   等于已存 parquet"的对照抓到。
2. **衰减方向写反**：`exp(-u)` 乘事件 / `exp(+u)` 缩放行 → 实际**给越老的事件越大权重**。
   若非单测断言"目标突变 50→90 后衰减估计须更接近 90"，会得出**完全相反**的结论
   （第一轮扫描就是这样，显示"遗忘有害"）。

### 8.4 响应剖面在跨玩家迁移中失效（重要边界）

读既有 `transfer_results_*.json`，M2+resp 相对 base：

| k | 1 | 5 | 10 | 20 | 50 | 100 | 200 |
|---|---|---|---|---|---|---|---|
| Δ（负=更好） | **−3.41** | **+3.01** | +0.75 | +0.54 | +0.15 | −0.09 | −0.36 |

**k=5..50 变坏，而这正是新玩家最实际的区间**；既有 `RESP_LAMBDA=20` 收缩只补回一半
（k=5：20.51 → 18.97，仍劣于 17.50）。→ 响应剖面是**玩家内**特征，迁移场景的 conditioning
仍需另寻。

---

## 6. 下一步建议（按价值）

1. **把 `B_resp`（含 180d 半衰期）立为 `compare_nolevel.py` 的正式参照行**，使"最优配置"进入
   日常基线流程；
2. **多变量（ridge）响应**：目前 11 个轴各自单变量 OLS，相关轴（密度↔NPS）存在遗漏变量偏误；
   联合拟合应更干净——这是同一个估计器的下一个自然升级；
3. **迁移场景的 conditioning**：响应剖面在 k=5..50 有害（§8.4），需要一个在小 k 下不外推的
   conditioning（更强的收缩、或直接按 k 门控）；
4. **数据覆盖**（真正的天花板）：ST4-12 中高段玩家、短历史新玩家、与现有曲库高度重叠者；
5. 让 `B_resp` 的**推理工具**落地（持久化 + 新存档一键出三目标预测），这是"可用性"唯一缺的工程件。
