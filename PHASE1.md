# PHASE1 —— BMS 难度预测：从零到第一个可复现的实验基线（封存记录）

> 封存日期：2026-09-02
> 本文件按"问题 → 数据 → 假设 → 实验 → 结论 → 被否定的东西 → 最大未知"记录整个第一阶段的完整脉络。

## 我们原本想解决什么

最初的问题：**BMS 谱面难度是不是一个单一标量？能不能让机器学到比现有难度表更丰富的难度表示？**

源头来自一次头脑风暴（见 `头脑风暴 BMS难度*.md`）：不满足于现有体系——

* 传统难度表（★/☆/sl/st）把整张谱压缩成一个数字，丢失"难在哪"；
* IR/玩家成绩推定回答的是"这张谱大家打得怎么样"，而不是"这张谱要求什么能力"。

理想目标是建立一条链：

```text
Chart → Chart Representation → Skill Demand ⇄ Player Skill → Performance
```

难度表、推荐、训练谱都只是这条链上的应用。第一阶段（本文件）**不做玩家建模**，只做前半段：谱面 → 表示 → 难度。

## 拿到了什么数据

### 语料

* 小集：`testbms`（305 谱面，4 首歌，全差分）——用于搭管线。
* 大集：`F:\games\BMS`（171.5 GB，2,432,475 文件，**48,619 个谱面文件**，3420 个包目录）。
* 清洗后：**36,744 个干净 7K 谱面**、8,315 首不同歌曲（按基础曲名分组）、11,875 个隔离样本（DP 约 1.1 万、非 7K、控制流、MGQ-LN、无 note、PMS、bmson）。

### 标签

* 下载了 10 张社区难度表（beatoraja/BeMusicSeeker JSON 格式，按 MD5/SHA256 关联谱面）。
* 主数据集：**Satellite（sl0–sl12）**——质量过滤、双 hash、内部标尺一致。
* Satellite 有标签的干净 7K 谱面：**1,905 张**（排除 60 个极端 flag 后分析用 1,845）。
* 严格 song-level 3-way split：1,503 首歌 → train 1,288 / val 272 / test 285（同曲差分永远同侧）。

## 第一轮假设是什么

1. **结构化统计特征能预测难度**：26 个 chart-level 统计量（note 数、时长、密度、BPM、和弦、jack、各 lane 计数）在严格 song split 下应该能解释 Satellite 难度的大部分方差。
2. **序列里可能有统计特征之外的信息**：难度也许藏在 note 的"排列顺序"里（jack/trill/楼梯等局部 pattern），统计特征把它们压扁了。
3. 操作化：**局部窗口 → 整谱难度**——如果一小段谱面就能判断整谱大致难度，说明难度信息是局部可读的。

## 做了哪些实验

### 0. 地基：parser 正确性

对照 bms-js 参考实现逐场景验证时间轴，25 个单元测试。修复 5 个真实 bug：
STOP 单位（value/48 拍）、头命令正则（#STOP11 被拆坏）、base36 大小写、LNOBJ 重复计数、#BPM 0 除零崩溃。

### 1. 26 特征分析（Satellite）

| 模型 | test MAE | R² |
|---|---|---|
| mean | 3.382 | ≈0 |
| 线性回归 | 1.186 | 0.849 |
| MLP（26→64→32→1，5 种子） | 1.064–1.096 | 0.859–0.869 |

单变量最强：peak_nps_1s（ρ=0.87）、avg_nps（0.83）、total_notes（0.78）。
消融：chord/density ≈ 规模 > lane ≫ BPM/LN（后两者≈0）。
残差集中在高密度、高难度谱面。

### 2. 残差 + 序列聚合统计（负结果）

从 note sequence 提取 17 个聚合量（密度变化/间隔结构/lane 移动/chord 形状），
与 |残差| 做偏相关（控制 sl）——全部 ≤0.14；26+5 特征实验反而更差（1.115 vs 1.059）。
**简单聚合统计不携带超出 26 特征的独立信息。**

### 3. Sequence CNN 第一版（负结果 + 关键转折）

窗口=512 event（非重叠，同谱面窗口同 split）。同一窗口下 26 特征 MLP（B_window）能到 1.08，
而 CNN 只有 2.62——**失败不是数据/标签噪声，是模型没提取出来**。

### 4. 输入表示：delta_t（重大转折）

| 表示 | chart MAE | 窗口时长辅助任务 R² |
|---|---|---|
| [time, lane, type, duration] | 2.62 | 0.39 |
| [delta_t, lane, type, duration] | **1.09** | **0.999** |

absolute timestamp 是主要表示问题；delta_t 让 CNN 追平统计特征。

### 5. 感受野

2×k3=1.076 / 4×k3=1.088 / **2×k5=1.023**。加深无收益，第一层加宽核有小而一致的收益（总体首次低于 MLP）。

### 6. Chord grouping（负结果，有混淆）

同时间点合并为一个事件（10 维）：1.097 vs 1.064，总体略差、低难度带变好、高难带变差。
混淆因素：窗口数减半、丢失 chord 内 note 数/时长。

### 7. Lane 编码与 lane shuffle（关键否定）

* lane one-hot vs 整数：1.052 vs 1.050——无差异。
* **lane shuffle**（整谱打乱 lane，保留 delta_t/type/duration 与 lane 频率）：1.052 vs 1.058——**无差异**。

## 实验告诉了我们什么

1. **26 个统计特征本身已经很强**：严格 song split 下 R²≈0.88、MAE≈1.0（sl 标尺 0–12）。
2. **第一版 CNN 的失败是表示问题，不是模型问题**：换成 delta_t 后，序列 CNN 从 2.62 追平到 1.09。
3. **序列 CNN 目前追平但没有超过统计特征**：说明"排列顺序里额外的难度信息"还没有被当前模型拿到。
4. **当前模型的预测力几乎全部来自时间/density 结构**：打乱 lane 排列性能不变。
5. 残差（约 1 个 sl 等级）主要落在高密度、高难度谱面（sl5–8、sl9–12）。

## 哪些东西被否定

* ❌ **简单序列聚合统计（17 个量）作为新特征**——无额外信息（偏相关 ≤0.14）。
* ❌ **absolute time 作为序列输入**——是主要表示问题。
* ❌ **加深 CNN（4×k3）**——无收益（参数量 2.3 倍换不来提升）。
* ❌ **chord grouping 表示**——总体略差（且被混淆因素污染，无法干净归因）。
* ❌ **lane one-hot**——与整数无差异。
* ❌ **"lane 排列携带当前 CNN 可用的难度信号"**——lane shuffle 后性能不变。
* ❌ **"第一版序列模型能自动超过统计特征"**——v1 远差于统计；修正表示后才追平，未超过。

## 目前最大的未知

**在密度之外，note 的排列顺序里到底有没有当前模型没拿到、但真实存在于难度标签里的信息？**

具体地：

1. lane shuffle 说明当前 CNN 不在乎 lane 排列——但这可能是**模型不敏感**，而不是**信息不存在**。
2. 已知的尝试（聚合统计、chord grouping、one-hot、加深/加宽）都没能让序列表示超过 26 特征；
   尚未尝试的是"能让模型真正利用排列结构的表示/模型"（这超出了本阶段范围，按约定未做）。
3. 另一个隐藏变量是**标签本身**：Satellite 相邻等级之间的差异可能本身就有噪声，
   "MAE=1" 的天花板可能部分是标签粒度的下限，而不是模型的极限。

一句话：**我们不知道"pattern 结构"（jack/trill/楼梯等）是否携带统计特征之外的真实难度信号——这是下一阶段要回答的问题，而本阶段的全部产物都是为了回答它准备的地基。**

---

### 产物与复现

* 代码：`bms_ml/`（26 个模块，含全部实验脚本）。
* 数据产物：`bms_ml/output/corpus/`（manifest、审计、隔离、去重、序列 npy）。
* 实验产物：`bms_ml/output/corpus/analysis/`（split/features schema/各轮 results.json/图表/checkpoint）。
* 归档总览：`PROJECT_ARCHIVE.md`；复现命令见 `bms_ml/README.md`。
